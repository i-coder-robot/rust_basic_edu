fn main() {
    //使用bool关键字声明一个布尔值，取值，true,false
    let checked:bool = true;
    println!("checked {}",checked);
    //字符： 是字符串的基本组成，也就是单个字符或字，UTF-8作为底层编码，包含数字，字母，Unicode和其他特殊字符。
    let c='R';
    println!("c {}",c);
}
